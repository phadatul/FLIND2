There are 4 classes named TextNote.java, TextAndImageNote.java, NoteStore.java and Launcher.java.

TextNote.java has two constructors, one default and one parameterized with text as the parameter. It also has one toString Method which prints the text.

TextAndImageNote.java has two constructors, one default and one parameterized with text,url as the parameter. It also has one toString Method which prints the text and url.

NoteStore.java has two ArrayList one for storing TextAndImage Note and one for storing TextNote. It has one parameterized constructor which intializes the Arraylists. It has the method storeNote and getAllTextNotes which adds and returns TextNotes and TextandImagesNotes respectively.

Laucher.java contains the main function which shows the notes and notes and url respectively.